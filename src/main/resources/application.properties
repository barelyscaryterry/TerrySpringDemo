# Enable Spring DevTools properties
spring.devtools.restart.enabled=true
spring.devtools.livereload.enabled=true

# Server configuration
server.port=8080

# Logging configuration
logging.level.com.example.demo=DEBUG
